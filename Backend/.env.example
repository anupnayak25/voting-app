## Copy this file to .env (do NOT commit the real .env) and fill in secure values

# Mongo connection string (include db name at end, e.g., /voting-app)
MONGO_URI=mongodb+srv://mrnayak27_db_user:XzE1IpZ2oy9JW1on@cluster0.aogttrg.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0/voting-app
# Server
PORT=5000

# App configuration
REGISTRATION_DUE_DATE=2025-09-10
ADMIN_PASS=changeme123
JWT_SECRET=dev_jwt_secret_change_me

# Voting window (ISO timestamps or leave blank to disable enforcement)
VOTING_START=2025-09-20T09:00:00.000Z
VOTING_END=2025-09-20T17:00:00.000Z

# Email (choose ONE of the two approaches below)
# 1. Gmail + App Password (account must have 2FA; generate 16-char app password w/out spaces)
EMAIL_USER=nnm24mc014@nmamit.in
EMAIL_PASS="rwmw shug bfqr nyyd"

# 2. Custom SMTP (uncomment and fill to override Gmail service)
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=2525
# SMTP_SECURE=false
# EMAIL_USER=<smtp-username>
# EMAIL_PASS=<smtp-password>


